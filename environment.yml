name: tar-project
channels:
# order matters, channels are tried sequentially starting from the first one
  - pytorch
  - nvidia
  - conda-forge
  - anaconda
dependencies:
  - python=3.9
  - numpy=1.22.3
  - matplotlib=3.5.2
  - nvidia::cudatoolkit=11.3
  - pytorch::pytorch=1.11.0
  - scikit-learn=1.1.0
  - pandas=1.4.2
# uncomment the stuff below when first pip package is needed
#  - pip=21.2.4
#  - pip:
#     - list_pip_packages_here
